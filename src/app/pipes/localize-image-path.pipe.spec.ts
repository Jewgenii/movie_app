import { LocalizeImagePathPipe } from './localize-image-path.pipe';

describe('LocalizeImagePathPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalizeImagePathPipe();
    expect(pipe).toBeTruthy();
  });
});
