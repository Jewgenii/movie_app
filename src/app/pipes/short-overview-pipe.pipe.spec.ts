import { ShortOverviewPipe } from './short-overview-pipe.pipe';

describe('ShortOverviewPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortOverviewPipe();
    expect(pipe).toBeTruthy();
  });
});
